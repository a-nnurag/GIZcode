# Base URL of the gated backend (see server/) that now serves all map data.
# Copy this file to .env.local (gitignored) for local dev against a locally-run backend.
VITE_API_BASE_URL=http://localhost:8787
